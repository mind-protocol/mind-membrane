# Infrastructure helpers

This directory is reserved for future Dockerfiles, tunnel scripts, and
infrastructure-as-code assets that package the Mind Membrane MCP server for
local development and deployment. For the initial delivery the runtime can be
launched directly with the npm scripts exposed in `apps/server`.
